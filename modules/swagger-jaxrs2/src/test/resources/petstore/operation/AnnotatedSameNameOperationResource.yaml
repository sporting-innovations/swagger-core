openapi: 3.0.1
paths:
  /sameOperationName:
    get:
      description: Same Operation Name
      operationId: sameOperationName
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string
  /sameOperationNameDuplicated:
    get:
      description: Same Operation Name Duplicated
      operationId: sameOperationNameDuplicated
      requestBody:
        content:
          '*/*':
            schema:
              type: string
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string

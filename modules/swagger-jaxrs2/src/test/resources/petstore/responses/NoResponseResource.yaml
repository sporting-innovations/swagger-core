openapi: 3.0.0
paths:
  /pets:
    get:
      summary: Find pets
      description: Returns the Pets
      operationId: getPets
      responses:
        default:
          description: default response

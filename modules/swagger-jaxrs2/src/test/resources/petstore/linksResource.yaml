openapi: 3.0.0
paths:
  /users:
    get:
      operationId: getUser_1
      parameters:
      - name: userId
        in: query
        schema:
          type: string
      responses:
        default:
          description: test description
          content:
            '*/*':
              schema:
                type: string
          links:
            user:
              operationId: getUser
              parameters:
                userId: $request.query.userId

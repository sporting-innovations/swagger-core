openapi: 3.0.0
paths:
  /users:
    get:
      operationId: getUser
      responses:
        default:
          description: default response
          content:
            '*/*':
              schema:
                type: string
